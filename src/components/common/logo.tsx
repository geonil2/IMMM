import React from 'react';
import { Link } from 'react-router-dom';

const Logo = () => {
    return (    
        <Link to="/"><img className="logo-img" src="" alt="Logo image" /></Link>
    );
};  

export default Logo;